# 魔方网表权限查看器环境变量配置

# TOKEN鉴权配置
# 设置为 true 启用TOKEN鉴权（跨域部署模式）
# 设置为 false 禁用TOKEN鉴权（同源部署模式，依赖会话继承）
VITE_USE_TOKEN_AUTH=false

# 已有TOKEN（本地调试优先使用，支持带或不带 Bearer 前缀）
VITE_AUTH_TOKEN=

# 用户名（启用TOKEN鉴权时需要）
VITE_AUTH_USERNAME=

# 密码（启用TOKEN鉴权时需要）
VITE_AUTH_PASSWORD=
